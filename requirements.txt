requests==2.31.0
beautifulsoup4==4.12.2
# torch >= 2.6 diperlukan agar transformers 5.x mau memuat pytorch_model.bin
# (CVE-2025-32434). Di Windows, torch dari PyPI adalah build CPU (~120 MB).
torch==2.14.0
sentence-transformers==6.1.0
transformers==5.17.0
chromadb==1.5.9
# Klien Gemini (Interactions API) untuk lapisan generasi jawaban
google-genai==2.24.0
# Data zona waktu (America/Los_Angeles) untuk reset kuota harian; Windows tak punya bawaan
tzdata==2026.4
# Antarmuka demo (src/app.py); hanya menambah paket, tidak mengubah versi paket lain
streamlit==1.64.0
