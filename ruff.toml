# Konfigurasi ruff proyek. Aturan bawaan ruff (versi dikunci di requirements-dev.txt)
# dipakai apa adanya; di sini hanya pengecualian yang disengaja.

[lint.per-file-ignores]
# src/app.py SENGAJA mengimpor `ingest` sebelum paket pihak ketiga (chromadb,
# streamlit): `ingest` men-set DISABLE_SAFETENSORS_CONVERSION=1 sebelum transformers
# termuat. Tanpa itu transformers menjalankan Thread non-daemon yang mengunduh varian
# safetensors bge-m3 (2,3 GB) dan proses menggantung (lihat CLAUDE.md, "Jebakan").
# Mengurutkan ulang impor sesuai I001 akan merusak jaminan urutan itu.
"src/app.py" = ["I001"]
